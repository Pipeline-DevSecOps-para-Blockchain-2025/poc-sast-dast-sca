name: Build Aderyn Image

on:
  workflow_dispatch:
    inputs:
      aderyn-version:
        description: 'Aderyn version'
        required: true
        default: '0.6.8'
      latest:
        description: 'Mark as latest'
        required: true
        type: boolean
        default: false

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set image name
        id: image_name
        run: echo "image_name=ghcr.io/$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')/aderyn" >> $GITHUB_OUTPUT

      - name: Log in to the GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        id: build-and-push
        uses: docker/build-push-action@v6
        with:
          context: ./docker/aderyn
          push: true
          build-args: |
            ADERYN_VERSION=${{ github.event.inputs.aderyn-version }}
          tags: |
            ${{ steps.image_name.outputs.image_name }}:${{ github.event.inputs.aderyn-version }}
            ${{ github.event.inputs.latest && format('{0}:latest', steps.image_name.outputs.image_name) || '' }}
